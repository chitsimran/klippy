import React from 'react';
import { LogoContainer, LogoText } from './styles';

const Logo = () => {
    return (
        <LogoContainer>
            <LogoText>
                klippy
            </LogoText>
        </LogoContainer>
    );
}
 
export default Logo;
